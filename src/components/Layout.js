import React from 'react';
import '../styles/reset.scss';
import '../styles/main.scss';

const Layout = ({ children }) => {
	return (
		<>
		<h1>The Info Site999</h1>
		{children}
		</>
	)
}

export default Layout;